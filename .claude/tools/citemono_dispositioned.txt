# Inversion groups that have been EXAMINED and EXPLAINED. Format:
#     <odin_proc> | <cpp_function> | <reason>
# citemono.py reports these separately so they stop consuming a reader's attention, exactly as a
# cleared --suspect cluster must be ANCHORED rather than left bare (#605). Adding a line here is a
# claim that the inversions were investigated and are NOT defects -- state the evidence in the reason.

check_expr_base_internal | check_expr_base_internal | SWITCH-ARM REORDERING, verified #611. Both implementations dispatch over AST node kinds and the ARM ORDERS DIFFER SUBSTANTIALLY -- 41 of 78 common-arm pairs are discordant. C++ (case_ast_node macros) runs BadExpr, HelperType, Implicit, Ident, Uninit, BasicLit, ...; the port runs Basic_Lit first and Bad_Expr LAST, with Implicit 22nd against C++'s 3rd. Spot-checked six of the 23 inverted citations by CONTENT and every one is exactly right: Implicit->12325, Undef->12366 (Uninit), Proc_Group->12401, Ternary_If->12450, Paren->12475, Tag->12481. The monotonicity premise (port order tracks C++ order) simply does not hold for switch dispatch.

check_type_decl | check_type_decl | ENTIRELY FLOOR, verified #611b/#666. All 20 citations were read and four were CORRECTED with the count UNCHANGED, which proved the residual 18 are the port's REAL ordering, not citation drift. Both halves are inert: (a) the attribute block runs before C++'s equivalent, but C++'s DECL_ATTRIBUTE_PROC (checker.hpp:179) takes NO ENTITY PARAMETER, so type_decl_attribute (checker.cpp:4464-4558) only validates VALUES and fills `ac` -- 11 `ac->` refs vs 2 entity refs, both LOCALS from the attribute's own value; every entity-dependent read runs AFTER, off `ac`. (b) the raddbg/'using' swap shares no state and only ONE side emits, so no swap can reorder output. Two ticks, four citations fixed, ZERO defects. NOT INVESTIGATED: a self-referential attribute value (@(objc_superclass=Self)) resolving while the entity type is nil -- goes through the scope, unlikely, unmeasured.

check_builtin_procedure_directive | check_builtin_procedure_directive | NAME-DISPATCH ARM ORDER, inert -- verified #667/#669/#670. The port dispatches `if name == "..."` with `name` assigned EXACTLY ONCE (check_builtin.odin:3313, regex positive-control-verified), so the arms are mutually exclusive regardless of returns and their order cannot affect output; C++ uses the same chain in a different order. THE INVERSION COUNT CARRIES NO DEFECT SIGNAL. But the ARMS are a different question and were read separately: ELEVEN live defects were found by READING (#667 config x3, #670 defined+load x4, plus #668's four in the neighbouring bare-directive path), NONE of them pointed at by this metric. caller_expression/assert/panic/load_hash/hash verified CLEAN in text AND anchor. exists has a third guard C++ lacks -- FILED as #669, latent, re-test on #7268. RE-READ #793 (last never-re-read group; the re-read pass is now COMPLETE): citations CLEAN -- all 25 function-named citations land inside C++ 2415-2776, plus an explained OLD VINTAGE of 5 bare citations at drift +326 (2089+326 = 2415 = the function's EXACT start; :3351's 2093-2112 +326 = 2419-2438 = the #location arm exactly). Arm sets MATCH 11 vs 11. :3294 and :3307 are a DUPLICATE doc-header pair carrying the same range (rule 10, third instance after #777/#788/#792), and that range is stale twice over -- correct start, but 2175+326 = 2501 is 275 SHORT of the real end 2776. The doc header's "Implemented directives" list names 7 of 11 (#load_hash/#hash/#load_directory added later by LEDGER #249/#229/#248, header never updated). :3887's copy-paste-slip claim VERIFIED EXACTLY: C++ 2521 AND 2591 both print type_to_string(o.type) while validating o_hash, and the port mirrors the slip deliberately at BOTH sites (:3813, :3889). Cross-function citations (check_load_directive 2152-2176 @2146, check_load_directory_directive 2219-2330 @2219, 2191) are all CURRENT and CORRECT -- second confirmation of #786. STRUCTURAL FINDING, OPEN: C++'s single check_load_directive (2146-2209, TWO callers) was SPLIT into TWO LIVE PORT COPIES -- a faithful one at check_expr.odin:55 (one caller, the or_else path, err_on_not_found=false) and an INLINE ~77-line reimplementation at check_builtin.odin:3697 (the directive arm, err_on_not_found=true). The doc header at :3305 asserts the opposite and is FALSE. THE COPIES ALREADY DIVERGE: C++ sets operand->type (2179) and operand->mode (2197) BEFORE the cache load so they survive the NotFound return, check_expr.odin's copy does the same, and the inline copy assigns the operand ONLY inside `if load_ok && cache != nil`. #633's shape INVERTED -- there the faithful port is dead, here BOTH are live. NOT REPRODUCED (#178). Filed COMPILER_ISSUES/CHECKER_ISSUES/793-load-directive-duplicated-implementation.md. Awaiting Jon.

check_basic_directive_expr | check_basic_directive_expr | NAME-DISPATCH ARM ORDER, inert -- same shape and same proof as check_builtin_procedure_directive. Read end to end in #668: four live defects found (missing `location` arm, invented `panic` arm, INVENTED `#column` arm accepting source C++ rejects, and `#procedure` out-of-proc diverging in text AND recovery), all fixed and oracle-verified. Controls #file/#directory/#line/#caller_location/#branch_location all hold. The remaining inversions are arm order only.

check_matrix_type_expr | check_matrix_type | TWO DELIBERATE STRUCTURAL DIFFERENCES, both documented at the site, and the DIAGNOSTIC order is now C++'s. Read end to end in #676, which found and fixed two live defects here first (element-validity block ran before the count checks and won the same-position merge, suppressing the real column-count diagnostic; and the maximum-elements error was anchored at the column count where C++ uses the whole matrix node). AFTER those fixes 11 inversions remain over 14 citations, and every one of them hangs off exactly two anchors. (1) `after 3139` -- the port resolves the ELEMENT TYPE at the top of the function (check_type.odin:3797) where C++ resolves it at check_type.cpp:3139, late. The port must, because it publishes a partially-built matrix type to the caller before validation so that a SELF-REFERENTIAL named matrix declaration has its base set; the comment at check_type.odin:3782 records why, and every error path overwrites the publish with t_invalid. C++ avoids the whole problem by allocating once at :3158 with all fields known. (2) `after 3158` -- the port assigns row_count / column_count / generic_* / is_row_major into that pre-allocated type incrementally, so each field assignment cites the single C++ alloc_type_matrix call at :3158, which necessarily sits after every count citation. Neither anchor is an emission: (1) is a type resolve and (2) is a field store, so #64's test ("does the reordered block EMIT?") answers no for both, and #92's follow-up ("does it share an anchor with something it now precedes?") does not arise. Pinned by six corpus members p676a/b/c/d/e/f covering the merge collision, the anchor, and a no-collision control. All C++ line numbers in this function were also corrected here: they were uniformly stale by about -4 (3095/3101/3108/3118/3124/3128/3149/3154 -> 3099/3105/3113/3123/3128/3133/3153/3158), a comment-only change proven inert by the #50 code() test at 0 bytes.

check_type_assertion | check_type_assertion | UNIFORM DRIFT (+61..+93) PLUS ONE HOISTED-HELPER DOC COMMENT -- every inversion dissolves, verified #773/#780. 3 inversions over 6 citations. **check_type_assertion BEGINS at check_expr.cpp:11665**, and TWO citations point OUTSIDE it: :7114 cites 11659 (`o->mode = Addressing_Value;` in the PRECEDING function) and :7163 cites 11657-11665 (the previous function's tail plus this one's signature line). Each comment's own quoted text was grepped for its real home: :7114's ``goto end`` is at **11720** (+61); :7163 quotes the "Cannot type assert '%s' to '%s' as this is an empty union" message, at **11755** (~+95); :7190 quotes `} else if (is_type_any(src)) { o->type = t; ... }`, at **11769** (+80); :7230 emits "Type assertions can only operate on unions and 'any', got %s", at **11777** (+93). **CORRECTED ORDER IS 11720, 11755, 11769, 11777 -- STRICTLY ASCENDING, matching the port's :7114, :7163, :7190, :7230 exactly. Every numeric inversion dissolves; there is no reordering.** The remaining flagged item, :7259, is NOT in this procedure at all: it is the DOC COMMENT of `add_type_assertion_dependencies`, a separate top-level proc the port extracted because **Odin has no `goto`** and C++ reaches the `end:` block from THREE places (both `.?` success paths and the ordinary `x.(T)` fall-through); check_type_assertion CALLS it at :7252. That is the tool's own "hoisted helper cites call sites below it" exemption. **TELL-TALE worth reusing: :7259 sits at COLUMN 0 while the five real citations are indented inside the body.** PORT BEHAVIOUR NOT IN QUESTION -- this surface was fixed by #561 (`.?` skipped the type-assertion dependency tail; flags column went to 0 corpus-wide) and by the helper extraction itself. Drift RECORDED, NOT CORRECTED, per #739.

check_switch_stmt | check_switch_stmt | IF/ELSE ARM SWAP on `y.mode == Addressing_Type`, inert -- verified #778, and all seven citations THAT THIS TOOL EXTRACTS are accurate -- **SCOPED BY #784: the procedure carries at least EIGHT citations, and citemono's regex (`check_switch_stmt:NNNN`) CANNOT SEE the eighth. `:2273` reads "C++ lines 8995-9038: Duplicate case value detection", but check_stmt.cpp is 3084 LINES and check_switch_stmt spans 1150-1387 -- that range DOES NOT EXIST in the file. "Fully verified" here means verified against the TOOL'S EXTRACTION, not against the procedure.** Also open per #784: the port replaced C++'s `add_to_seen_map(ctx,&seen,y)` (1328, inside the else, after the `.Constant` guard) with its own block BELOW both branches gated `y.mode == .Constant && y.value != nil`; **#785 CLOSES THIS: `y.value != nil` is FAITHFUL, NOT INVENTED.** C++'s single-value `add_to_seen_map` (check_expr.cpp:9704) is a one-line wrapper over `add_constant_switch_case` (check_expr.cpp:9630), which OPENS with exactly two guards: `if (operand.mode != Addressing_Constant) return;` and `if (operand.value.kind == ExactValue_Invalid) return;`. The port's `y.mode == .Constant && y.value != nil` is those two guards HOISTED FROM CALLEE TO CALLER -- `ExactValue_Invalid` is C++'s no-value sentinel and the port's exact-value union spells it `nil`. No behavioural difference. **#785 also RE-TESTED the eighth citation under the alternate reading and it STILL fails: check_expr.cpp IS long enough (13,631 lines) to contain 8995, but 8995-9038 there is the `#force_inline`/`#no_inline`/`#must_tail` block, unrelated to duplicate-case detection. WRONG UNDER BOTH READINGS.** 2 inversions over 7 citations. check_switch_stmt begins at check_stmt.cpp:1150, so every cited line is inside it. C++ 1304-1329 is ONE `if (y.mode == Addressing_Type) { ... } else { ... }`: the TYPE arm is 1304-1312 (`error("Invalid type for case clause")` guard, then `t = default_type(t)`, `add_type_info_type`, and `add_type_to_seen_map` at **1312**), and the VALUE arm is 1314-1329 (`convert_to_typed`, `check_comparison`, `update_untyped_expr_type`, `add_to_seen_map`), carrying `// NOTE(bill): the ordering here matters` at 1319. Checked by content: :2243's 1303-1313 brackets the TYPE arm -- CORRECT; :2269's 1312 is exactly `add_type_to_seen_map`, which its comment calls "record the type for duplicate detection (#298)" -- CORRECT; :2199's 1314-1329 is the VALUE arm and its comment refers to the NOTE(bill) line that arm carries -- CORRECT. **The inversion is a pure ARM SWAP: the port emits the VALUE arm first and the TYPE arm second, where C++ writes `if (Type) ... else ...`.** INERT because the two arms are the branches of a single condition on `y.mode`: an operand either IS `Addressing_Type` or is not, so no input reaches both and order cannot decide which runs. Same family as the check_polymorphic_record_type two-arm artifact (#771) and the mutual-exclusivity arguments in #772/#776. **NOTE FOR THE READER: this is the THIRD consecutive group (#776, #777, #778) whose citations are accurate -- the "citations are always stale" impression from #770-#775 does NOT generalise.** LIMIT, STATED: mutual exclusivity is argued from the if/else structure, not machine-checked.

process_all_delayed_decls | check_import_entities | DOC-HEADER ENUMERATION RE-CITED BY THE BODY -- a NEW artifact class, inert by construction, verified #777. 2 inversions over 6 citations. The proc's DOC COMMENT enumerates the three phases with a citation each (:1557 the whole function 6228-6310; :1560 phase 1 imports 6235-6238; :1561 phase 2 foreign blocks 6284-6296; :1562 phase 3 directive exprs 6303-6307), and then the BODY re-cites the same lines at the actual call sites (:1566 -> 6235-6238, :1569 -> 6284-6296). The header runs 6235 -> 6284 -> 6303 and the body restarts at 6235, so the "inversion" is manufactured entirely by documentation duplication. **A DOC COMMENT IS NOT CODE; there is no ordering claim to violate.** This class will recur wherever a proc's header lists its phases and the body repeats the references -- recognise it by the SAME RANGE appearing twice, once above the signature and once inside. Note also that only phases 1 and 2 repeat: **phase 3 is DELIBERATELY NOT RUN HERE, and that is the #111 fix, not a missing phase.** C++ drains the Expr queue inside check_import_entities (6303-6307), which is AFTER check_export_entities_in_pkg (6119) has published entities into the package scope; this proc runs during check_collect_entities_all, i.e. BEFORE any export, so a file-scope `#assert(size_of(T) == N)` evaluated here cannot see types from other files of the same package -- which silently cached a WRONG SIZE for the rest of the run (`core/sys/linux/types.odin`'s `#assert(size_of(IO_Uring_SQE) == 64)` made `linux.Mode` measure 1 instead of 4, degrading every dependent `bit_set` to `bit_set[0..=0]`). The drain happens in check_delayed_expressions_all instead (see check_files.odin). **DO NOT "restore" phase 3 here.** Drift RECORDED, NOT CORRECTED, per #739.

check_unary_expr | check_unary_expr | SWITCH-ARM REORDERING on `op.kind`, inert -- verified #776. 2 inversions over 5 citations. check_unary_expr begins at check_expr.cpp:2923, so all five cited lines are inside it. The last four citations ASCEND PERFECTLY (:2730 -> 2930-2957, :2767 -> 2976-3006, :2803 -> 3004-3013, :2823 -> 3008-3016); the ONLY thing breaking monotonicity is the LEADING :2675, which cites 2995-3035 for the `**x` expand_values operator. Verified by content: `case Token_MulMul` is at **3034** -- the cited range brackets it but starts 39 lines early, so the range is imprecise though its tail does contain the real line. The inversion therefore SURVIVES drift correction (3034 genuinely follows 2930 and 2976), so this is a REAL reordering: the port hoists the `.Mul_Mul` branch to the TOP of the procedure, above the address-of arm. INERT because C++ 3034 is an arm of a `switch (op.kind)` whose preceding (address-of) arm ends with `return;` at 3031, and the port's hoisted branch is guarded by `if op.kind == .Mul_Mul` -- a token kind cannot simultaneously be `.Mul_Mul` and `&`, so no input reaches both and order cannot change which fires. Same class as the check_expr_base_internal and check_builtin_procedure_directive entries. The port's own comment ("handled before check_unary_op") is CORRECT: `check_unary_op` is called at **3077**, after the switch, so MulMul is genuinely handled before it in C++ too; the port merely moves it earlier WITHIN the pre-check_unary_op region. Port's branch opens `if o.type == nil { return }`, matching C++ 3035-3037 `if (!o->type) { return; }`. LIMIT, STATED: mutual exclusivity is argued from the switch/guard structure, not machine-checked exhaustively. Drift RECORDED, NOT CORRECTED, per #739.

check_struct_type | check_struct_type | PURE CITATION DRIFT -- the inversion DISSOLVES COMPLETELY once the numbers are corrected, verified #775. 2 inversions over 4 citations. check_struct_type begins at check_type.cpp:659, so all four cited lines are genuinely inside it (unlike the #773 case). Each comment's own claim was grepped for its real home: :1126 "signal that polymorphic processing is complete" cites 665 (`isize min_field_count = 0;`) but `wait_signal_set(&struct_type->Struct.polymorphic_wait_signal)` is at **694** (+29); :1186 `sync.wait_group_done(&st.fields_wait_signal)` cites 681 (`struct_type->Struct.is_raw_union = true;`) but `wait_signal_set(&...fields_wait_signal)` is at **725** (+44); :1202 emits "'#%s' cannot be applied with '#packed'" and cites 686 (`struct_type->Struct.scope = ctx->scope;`) but that exact message is at **730** (+44). The fourth, :1153 (711-723, the `#simple` field validation from LEDGER #312), is CORRECT -- the partial-correction signature seen in #770/#771/#772/#774. CORRECTED ORDER IS 694, 711-723, 725, 730 -- STRICTLY ASCENDING, matching the port's :1126, :1153, :1186, :1202 exactly. **So there is no reordering and nothing to explain: the port's sequence is C++'s sequence, and the inversion was manufactured entirely by three stale numbers.** Note :1202 sits inside the NESTED `check_align` proc defined at :1196, which would have been the tool's own "hoisted helper" exemption had the drift not accounted for it first. Drift RECORDED, NOT CORRECTED, per #739.

check_is_castable_to | check_is_castable_to | DELIBERATE PORT REORDERING over MUTUALLY EXCLUSIVE guards, inert -- verified #772. 3 inversions over 7 citations. The drift had to be corrected FIRST to even ask the question: ALL SEVEN citations are stale by +35..+40, established by grepping each comment's own quoted predicate for its real home -- :9869 3713-3716 -> 3752-3755 (`is_type_u16_slice(src) && is_type_string16(dst)`), :9898 3759-3785 -> 3799-3823 (the six cstring16 POINTER rules), :9925 3722-3728 -> 3757-3762 (`cstring -> string` + its `add_package_dependency(c,"runtime","cstring_to_string")` at 3760), :9937 3725-3731 -> 3764-3769 (`cstring16 -> string16` + `cstring16_to_string16` at 3767), :9941 3729-3735 -> 3767, :9950 3815-3836 -> 3854, :9968 3827-3832 -> 3866. The cited lines are NOT the claimed code at all: 3713-3716 is the MATRIX square-count return and 3722-3735 is the pointer/uintptr cast block. CORRECTING THE DRIFT DOES NOT REMOVE THE INVERSION -- the real positions in port order are 3752, 3799, 3757, 3764, 3767, 3854, 3866, so :9898 genuinely precedes its neighbours. CAUSE: the port hoists the whole six-rule cstring16 POINTER block (C++ 3799-3823) ABOVE the cstring->string / cstring16->string16 CONVERSIONS (C++ 3757-3769), grouping every UTF-16 rule together; the comment at the site says so ("the UTF-16 counterparts of the block above"), and the block dates from task 114. INERT because check_is_castable_to is a flat chain of independent `if (<concrete type pair>) return ...;` guards: each tests NAMED concrete types (t_cstring / t_cstring16 / t_string / t_string16) or a specific pointer shape (u8/u16 ptr, multi_ptr, rawptr), so no (src,dst) pair can satisfy two of them and the order cannot change which one fires or what it returns. LIMIT, STATED: mutual exclusivity is argued from reading the predicates, not machine-checked exhaustively; the drift is RECORDED, NOT CORRECTED, per #739.

check_call_arguments_internal | check_call_arguments_internal | FORWARD-REFERENCING RATIONALE, inert. All 5 remaining inversions (over 11 citations) hang off ONE anchor: the arity-check comment block at check_proc_group.odin:789-816 cites check_expr.cpp:6962 TWICE, in prose that explains WHY the arity check sits where it does -- C++'s single check both emits the message and sets `err = CallArgumentError_TooManyArguments`, which the instantiation gate at :6962 then consults (LEDGER #510). The citation is a RATIONALE naming a downstream site, not an anchor for the code beneath it; the anchor for that code is :6706-6723, which is correct and precedes everything. Verified against C++ before dispositioning per #101, and the port's structure was read end to end in #681: operand array built (C++ :6697-6880) then instantiated (:6962-6975), then scored -- same order as C++, no reordering and no defect. The remaining cited lines 6697 / 6825 / 6903 are all genuinely BELOW 6962 in C++ and above it in the port only because the rationale sits at the top of the function. Converting the two rationale citations to the bare `check_expr.cpp:6962` form would clear the metric (citemono skips unanchored citations) but would ALSO drop them out of citefn's drift check -- a worse trade, deliberately not taken (#48). All C++ line numbers in this function were corrected here: 6684-6700 -> 6706-6723 (arity), 6940 -> 6962 x3 (instantiation gate), 6684-6960 -> 6697-6975, 6666-6700/6759-6788 -> 6697/6779-6820, 6835-6857 -> 6825-6879 (missing-required), 6845 -> 6846, 6879 -> 6903 (err-set-and-continue), 6970-6975 -> 6976-6983, 6992-6999 -> 7024-7046 (variadic loop), 6954-6958 -> 6976-6980 (invalid-operand skip). Comment-only, proven inert by the #50 code() test at 0 bytes. NOTE the drift was NOT uniform: -22 through most of the function but -32 at the variadic_operands loop. Both are C++-side, so unlike #680 the step measures UPSTREAM churn between the two snapshots, not a port/reference structural difference -- see #102.
check_polymorphic_record_type | check_polymorphic_record_type | TWO-ARM STRUCTURE (3 of 4) PLUS ONE INERT STATEMENT SWAP (1 of 4) -- verified #771/#782. The procedure has a STRUCT arm (:2482-:2535) and a UNION arm (:2548-:2591) mirroring C++'s `if (bt->kind == Type_Struct) ... else if (bt->kind == Type_Union)` at 8536/8546, and each arm walks C++ from its own starting point. **THE UNION ARM IS STRICTLY ASCENDING ON ITS OWN: 8456 < 8461 < 8462 < 8547.** Three of the four flagged inversions are the tool comparing :2535 (8547-8583) against :2548 (8456) ACROSS THE ARM BOUNDARY and then cascading; they are dispatch artifacts, not reorderings. **The fourth is real but inert.** Every citation in this group is stale by roughly +90: C++ 8432-8466 is the "replace missing operands with default values" loop and the scoring loop, NOT the instantiation code. Real homes found by grepping each comment's OWN QUOTED TEXT: :2517 quotes `ctx.scope = polymorphic_record_parent_scope(original_type)` verbatim = **8529** (cites 8438-8441); :2503 is the `polymorphic_parent` write = **8540** (cites 8451); :2566 is the union's `polymorphic_parent` write = **8550** (cites 8461); :2577 is `check_union_type` = **8554** (cites 8462-8464). **AFTER DRIFT CORRECTION THE :2503/:2517 INVERSION SURVIVES: C++ sets ctx.scope at 8529 BEFORE writing polymorphic_parent at 8540, the port writes polymorphic_parent (:2503) before building inst_ctx (:2517).** It is INERT: the two statements are independent -- one writes a field on the freshly allocated struct type, the other copies the caller's ctx into a local -- with no data dependency in either direction. The reorder exists because C++ hoists the ctx setup ABOVE the struct/union branch (8526-8529, shared by both arms) while the port duplicates it INSIDE each arm (:2529-2531 and :2583-2585). **BOTH port arms do have the parent-scope fix -- I predicted the union arm would be missing it and was WRONG.** TWO BEHAVIOURAL QUESTIONS ARE TRACKED SEPARATELY AND ARE *NOT* CLAIMED INERT HERE: (a) #771's nil guard -- C++ 8530 is `GB_ASSERT(ctx.scope != nullptr)` while the port writes `if parent_scope != nil`, silently keeping the caller's scope in the case C++ calls impossible; (b) #782 -- the port's STRUCT arm omits the `check_open_scope`/`check_close_scope` pair that C++ has around BOTH calls (8543-8545, 8553-8555) and that the port's OWN union arm has (:2587-2589). **#782 IS NOW CLOSED BY #783 AS NOT A DEFECT: the callee asymmetry mirrors the caller asymmetry. C++ check_struct_type:688 and check_union_type:775 both ADOPT the caller's scope (`.scope = ctx->scope`), so C++ must push in both arms; the port's check_union_type:2898 also ADOPTS (faithful), so its union arm must push -- but the port's check_struct_type CREATES its own at :1106 (`create_scope(ctx.scope,..)`) and PUSHES it at :1119, so its struct arm must NOT push, or there would be one scope too many. Both arms are correct by different routes.**
